---
phase: 06-question-bank-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Models/QuestionBankQuestion.php
  - app/Models/QuestionBankOption.php
  - app/Livewire/QuestionBank/Index.php
  - app/Livewire/QuestionBank/Create.php
  - app/Livewire/QuestionBank/Edit.php
  - app/Livewire/QuestionBank/Export.php
  - routes/web.php
  - database/migrations
autonomous: true
requirements:
  - QB-01
  - QB-02
  - QB-03
  - QB-04
  - QB-05
  - QT-01
  - QT-02
  - QT-03
  - QT-04
  - QT-05
  - EXP-01
  - EXP-02
  - EXP-03
  - PERM-01
  - PERM-02
user_setup: []

must_haves:
  truths:
    - Admin can view paginated list of all questions with type and marks display
    - Admin can create questions selecting from MCQ, True/False, Short Answer, Code Snippet types
    - Admin can edit any field of existing questions including type, options, correct answer, marks
    - Admin can soft-delete questions (moves to trash, can restore)
    - Admin can search questions by text content
    - MCQ questions support 2-6 options with multiple correct answer selection
    - True/False questions have fixed 2 options
    - Short Answer questions have no options (manual grading)
    - Code Snippet questions include code block textarea and language dropdown
    - Each question has editable marks/points field
    - Only admin role users can access question bank pages
    - Non-admin users receive 403 error for any question bank route
    - Export generates valid JSON file following ExamShield format
  artifacts:
    - app/Models/QuestionBankQuestion.php
    - app/Models/QuestionBankOption.php
    - app/Livewire/QuestionBank/Index.php
    - app/Livewire/QuestionBank/Create.php
    - app/Livewire/QuestionBank/Edit.php
    - app/Livewire/QuestionBank/Export.php
    - routes/web.php (question bank routes)
  key_links:
    - QuestionBankQuestion -> hasMany QuestionBankOption
    - Question types: MCQ, TRUE_FALSE, SHORT_ANSWER, CODE_SNIPPET
    - toExamShieldFormat() method -> JSON export
    - Admin role middleware -> PERM-01, PERM-02
---

<objective>
Create fully functional Question Bank module with CRUD operations, multiple question types, search, and JSON export. Move from online/ standalone into main app at app/Models and app/Livewire/QuestionBank.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-question-bank-module/06-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Reference implementations to adapt
@online/app/Models/QuestionBankQuestion.php
@online/app/Models/QuestionBankOption.php
@online/app/Livewire/QuestionBank/Index.php
@online/app/Livewire/QuestionBank/Create.php
@online/app/Livewire/QuestionBank/Edit.php
</context>

<interfaces>
<!-- Key types and contracts from reference implementations -->

From online/app/Models/QuestionBankQuestion.php:
```php
class QuestionBankQuestion extends Model {
    use SoftDeletes;
    
    protected $fillable = [
        'user_id', 'type', 'question_text', 'code_block',
        'code_language', 'marks', 'explanation',
    ];
    
    public function options(): HasMany { ... }
    public function creator(): BelongsTo { ... }
    public function toExamShieldFormat(): array { ... }
}
```

Question types (Enum values):
- 'MCQ' - Multiple choice with options
- 'TRUE_FALSE' - True or False
- 'SHORT_ANSWER' - Text response
- 'CODE_SNIPPET' - Code with language

From online/app/Models/QuestionBankOption.php:
```php
class QuestionBankOption extends Model {
    protected $fillable = [
        'question_id', 'option_text', 'is_correct', 'order',
    ];
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Copy and adapt question models to main app</name>
  <files>
    - app/Models/QuestionBankQuestion.php
    - app/Models/QuestionBankOption.php
  </files>
  <read_first>
    - online/app/Models/QuestionBankQuestion.php
    - online/app/Models/QuestionBankOption.php
    - app/Models/User.php (for relationship type hints)
  </read_first>
  <action>
    Copy models from online/app/Models/ to app/Models/ with these adaptations:

    1. QuestionBankQuestion.php:
       - Keep all existing fields and methods
       - Add User relationship type hint: `return $this->belongsTo(User::class, 'user_id');`
       - Ensure toExamShieldFormat() returns correct structure

    2. QuestionBankOption.php:
       - Keep all existing fields and methods

    3. Run: php artisan make:model QuestionBankQuestion --migration (if migration needed)
  </action>
  <verify>
    <automated>
      grep -q "class QuestionBankQuestion" app/Models/QuestionBankQuestion.php && \
      grep -q "class QuestionBankOption" app/Models/QuestionBankOption.php && \
      grep -q "toExamShieldFormat" app/Models/QuestionBankQuestion.php
    </automated>
  </verify>
  <done>
    - QuestionBankQuestion.php exists in app/Models/ with toExamShieldFormat()
    - QuestionBankOption.php exists in app/Models/
    - Models have correct relationships and casts
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Livewire components for Question Bank</name>
  <files>
    - app/Livewire/QuestionBank/Index.php
    - app/Livewire/QuestionBank/Create.php
    - app/Livewire/QuestionBank/Edit.php
  </files>
  <read_first>
    - online/app/Livewire/QuestionBank/Index.php
    - online/app/Livewire/QuestionBank/Create.php
    - online/app/Livewire/QuestionBank/Edit.php
    - app/Livewire (existing components for namespace pattern)
  </read_first>
  <action>
    Create Livewire components from online/ with adaptations:

    1. Index.php (list all questions):
       - Paginated list of questions with type and marks
       - Search by question_text
       - Delete/restore functionality
       - Uses card layout per D-01

    2. Create.php (add question):
       - Type selection dropdown (MCQ, TRUE_FALSE, SHORT_ANSWER, CODE_SNIPPET)
       - Conditional fields per D-02:
         * MCQ: shows options builder (2-6 options, multiple correct)
         * TRUE_FALSE: fixed True/False options
         * SHORT_ANSWER: no options field
         * CODE_SNIPPET: code_block textarea + code_language dropdown
       - marks field (integer)
       - explanation field

    3. Edit.php (edit question):
       - Same form as Create, populates with existing data
       - Allows changing type (careful with option migration)

    Use App\Livewire\QuestionBank namespace per Livewire v3.
  </action>
  <verify>
    <automated>
      ls app/Livewire/QuestionBank/*.php | wc -l
      # Should show 3 files
    </automated>
  </verify>
  <done>
    - Index.php lists questions with pagination and search
    - Create.php handles all 4 question types with conditional fields
    - Edit.php allows editing any question field
  </done>
</task>

<task type="auto">
  <name>Task 3: Create export functionality</name>
  <files>
    - app/Livewire/QuestionBank/Export.php
  </files>
  <read_first>
    - app/Models/QuestionBankQuestion.php (toExamShieldFormat method)
  </read_first>
  <action>
    Create Export.php Livewire component:

    1. Lists all questions for export selection
    2. "Export All" button generates JSON download
    3. Uses toExamShieldFormat() for proper ExamShield schema
    4. Returns downloadable JSON file per D-03 (download button)

    JSON format per EXP-02: ExamShield import format with:
    - type, question_text, marks, code_block, language, options[]
  </action>
  <verify>
    <automated>
      grep -q "toExamShieldFormat" app/Livewire/QuestionBank/Export.php
    </automated>
  </verify>
  <done>
    - Export.php provides JSON download functionality
    - JSON follows ExamShield import format
  </done>
</task>

<task type="auto">
  <name>Task 4: Add routes with admin permission</name>
  <files>
    - routes/web.php
  </files>
  <read_first>
    - routes/web.php (existing route patterns)
  </read_first>
  <action>
    Add question bank routes to routes/web.php:

    Route group with:
    - Middleware: auth, role:admin (per PERM-01, PERM-02)
    - Prefix: /admin/questions

    Routes:
    - GET /admin/questions -> Index (list)
    - GET /admin/questions/create -> Create (form)
    - POST /admin/questions -> Store (create)
    - GET /admin/questions/{question}/edit -> Edit (form)
    - PUT /admin/questions/{question} -> Update (edit)
    - DELETE /admin/questions/{question} -> Delete (soft)
    - POST /admin/questions/{question}/restore -> Restore
    - GET /admin/questions/export -> Export (JSON download)

    Non-admin users will receive 403 (handled by role middleware).
  </action>
  <verify>
    <automated>
      grep -q "QuestionBank" routes/web.php
    </automated>
  </verify>
  <done>
    - All 7 CRUD+export routes exist with admin role protection
  </done>
</task>

<task type="auto">
  <name>Task 5: Create database migration for tables</name>
  <files>
    - database/migrations/*_create_question_bank_tables.php
  </files>
  <read_first>
    - database/migrations (existing migration pattern)
  </read_first>
  <action>
    Create migration for question_bank_questions and question_bank_options tables:

    1. question_bank_questions:
       - id (bigIncrements)
       - user_id (foreignId)
       - type (string) - enum: MCQ, TRUE_FALSE, SHORT_ANSWER, CODE_SNIPPET
       - question_text (text)
       - code_block (text nullable)
       - code_language (string nullable)
       - marks (integer)
       - explanation (text nullable)
       - deleted_at (timestamp nullable)
       - timestamps

    2. question_bank_options:
       - id (bigIncrements)
       - question_id (foreignId)
       - option_text (string)
       - is_correct (boolean)
       - order (integer)
       - timestamps

    Run: php artisan migrate
  </action>
  <verify>
    <automated>
      ls database/migrations/*question_bank*.php
    </automated>
  </verify>
  <done>
    - Migration creates both tables with correct schema
  </done>
</task>

<task type="auto">
  <name>Task 6: Add navigation link for admin sidebar</name>
  <files>
    - resources/views (navigation component)
  </files>
  <read_first>
    - resources/views (existing admin navigation)
  </read_first>
  <action>
    Add "Questions" link to admin sidebar navigation:

    - Path: /admin/questions
    - Label: Questions
    - Icon: question-mark or similar
  </action>
  <verify>
    <automated>
      grep -ri "questions" resources/views | grep -i "nav\|sidebar\|menu" | head -1
    </automated>
  </verify>
  <done>
    - Navigation link present in admin sidebar
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → QuestionBankQuestion | Untrusted: user input in question_text, options, code_block |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-----------|-------------|---------------|
| T-QB-01 | Tampering | Create/Edit forms | mitigate | Validate all inputs: type enum, marks integer, options array |
| T-QB-02 | Information Disclosure | Index listing | mitigate | Admin role check on all routes (PERM-01) |
| T-QB-03 | Repudiation | Delete/Export | mitigate | Log actions with spatie/laravel-activitylog |
</threat_model>

<verification>
- Run: php artisan test --filter=Question
- Verify: All routes return 403 for non-admin users
- Verify: Export produces valid JSON
</verification>

<success_criteria>
- Admin can list, create, edit, delete, restore, search questions
- All 4 question types work with conditional fields
- Export produces valid ExamShield JSON
- Non-admin receives 403 on all routes
</success_criteria>

<output>
After completion, create `.planning/phases/06-question-bank-module/06-01-SUMMARY.md`
</output>