---
phase: 10
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - app/Livewire/TrainingCatalog.php
  - app/Livewire/TrainingDetail.php
  - app/Livewire/LearningPath.php
  - routes/web.php
autonomous: true
requirements: []
must_haves:
  truths:
    - Member can browse training catalog with filters
    - Member can view training details and enroll
    - Enrolled member can progress through modules
  artifacts:
    - path: "app/Livewire/TrainingCatalog.php"
      provides: "Member training catalog with search/filters"
    - path: "app/Livewire/LearningPath.php"
      provides: "Module progression and completion"
  key_links:
    - from: "TrainingCatalog"
      to: "TrainingDetail"
      via: "click card"
    - from: "TrainingDetail"
      to: "LearningPath"
      via: "enroll + continue"
---

<objective>
Create member-facing UI for viewing training catalog, training details, and learning path progress.
</objective>

<context>
@.planning/phases/10-learning-training-learning-paths-modules-lessons-progress-tr/10-RESEARCH.md
@.planning/phases/10-learning-training-learning-paths-modules-lessons-progress-tr/10-UI-SPEC.md
@.planning/phases/10-learning-training-learning-paths-modules-lessons-progress-tr/10-01-PLAN.md

# From UI-SPEC: Member UI Requirements

Training Catalog Page:
- Filter bar: category, difficulty, status (enrolled/not enrolled)
- Search input with icon
- Training card grid: thumbnail, title, category badge, difficulty badge, duration, enroll button
- Pagination or infinite scroll
- Enrolled indicator on cards

Training Detail Page:
- Hero section: title, description, category/difficulty badges, duration, instructor
- Progress bar (for enrolled users)
- Module list accordion: module title, duration, completion status
- Expand to show lessons
- "Mark as Complete" button per module
- Resource library section: PDF, video, link cards
- Action buttons: Enroll / Continue / View Certificate

Learning Path View (Enrolled):
- Sidebar: Module navigation with progress indicators
- Main content: Lesson content viewer
- Navigation: Previous / Next buttons
- "Mark Module Complete" button
- Progress percentage display
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TrainingCatalog page component</name>
  <files>app/Livewire/TrainingCatalog.php</files>
  <read_first>
- app/Livewire/EventList.php (existing member-facing list pattern)
- 10-UI-SPEC.md (filter bar, search, card grid, category/difficulty colors)
  </read_first>
  <action>
Create TrainingCatalog Livewire component:
- Properties: search, category_filter, difficulty_filter, status_filter (enrolled/not enrolled)
- Query: Training::published()->with('instructor')->latest()
- Filters: Apply category, difficulty, search (title/description) filters
- View: Grid of training cards with thumbnail, title, category badge (color per UI-SPEC), difficulty badge (color per UI-SPEC), duration, enroll button
- For enrolled users: show enrolled indicator on cards
- Pagination: Use Laravel's paginate(12)
- Actions: Click card navigates to TrainingDetail
- Enrollment check: Check if current user is enrolled in each training
  </action>
  <acceptance_criteria>
- TrainingCatalog.php exists with search, category, difficulty, status filters
- View renders training cards with category and difficulty color badges
- Shows enroll button or "Enrolled" indicator based on user status
  </acceptance_criteria>
  <verify>
    <automated>ls app/Livewire/TrainingCatalog.php 2>/dev/null</automated>
  </verify>
  <done>TrainingCatalog component created with filters and card grid</done>
</task>

<task type="auto">
  <name>Task 2: Create TrainingDetail page component</name>
  <files>app/Livewire/TrainingDetail.php</files>
  <read_first>
- app/Livewire/EventDetails.php (existing detail page pattern)
- 10-UI-SPEC.md (hero section, progress bar, module accordion, resource library)
- app/Models/Training.php (from plan 10-01)
  </read_first>
  <action>
Create TrainingDetail Livewire component:
- Mount: Accept training ID or slug from route
- Properties: training, is_enrolled, progress_percent, modules (ordered)
- Hero section: Title, description, category badge, difficulty badge, duration_hours, instructor name
- For enrolled: Show progress bar with percentage, module completion checklist
- Module accordion: Expandable list of TrainingModules with title, duration, completion status
- "Mark as Complete" button per module (for enrolled users)
- Resource library: Grid of resources from training->resources JSON with type icons (PDF/video/link)
- Action buttons: "Enroll Now" (if not enrolled), "Continue" (if enrolled and in progress), "View Certificate" (if completed)
- Enrollment action: Creates TrainingEnrollment record
  </action>
  <acceptance_criteria>
- TrainingDetail.php exists with hero, progress bar, module accordion
- Shows resources grid with type icons
- Enroll/Continue/View Certificate buttons based on user status
  </acceptance_criteria>
  <verify>
    <automated>ls app/Livewire/TrainingDetail.php 2>/dev/null</automated>
  </verify>
  <done>TrainingDetail component created with all sections</done>
</task>

<task type="auto">
  <name>Task 3: Create LearningPath page component for enrolled users</name>
  <files>app/Livewire/LearningPath.php</files>
  <read_first>
- app/Livewire/TrainingDetail.php (from task above)
- 10-UI-SPEC.md (sidebar navigation, lesson content, progress display)
- app/Models/ModuleProgress.php (from plan 10-01)
  </read_first>
  <action>
Create LearningPath Livewire component:
- Mount: Accept training_id, module_id (optional, defaults to first incomplete)
- Properties: training, current_module, modules (ordered), progress, has_previous, has_next
- Layout: Two-column - sidebar (left, 25%) + content (right, 75%)
- Sidebar: Module navigation list with:
  - Module title, duration, completion status icon
  - Progress indicator (green checkmark for completed, circle for in-progress, empty for not started)
  - Current module highlighted
- Main content: Current module content display with:
  - Module title, duration
  - Content (text/HTML from module->content)
  - Resources for this module (from module->resources JSON)
- Navigation: "Previous Module" / "Next Module" buttons at bottom
- "Mark Module Complete" button: Updates ModuleProgress to completed, calculates new progress
- Progress display: "X of Y modules completed (Z%)" at top
- Authorization: Must be enrolled to access; redirect to TrainingDetail if not enrolled
  </action>
  <acceptance_criteria>
- LearningPath.php exists with sidebar and content layout
- Shows module navigation with progress indicators
- Has Previous/Next navigation and Mark Complete button
- Progress percentage displayed
  </acceptance_criteria>
  <verify>
    <automated>ls app/Livewire/LearningPath.php 2>/dev/null</automated>
  </verify>
  <done>LearningPath component created with full learning experience</done>
</task>

<task type="auto">
  <name>Task 4: Add routes for training pages</name>
  <files>routes/web.php</files>
  <read_first>
- routes/web.php (existing routes pattern)
- app/Livewire/TrainingCatalog.php (from task 1)
- app/Livewire/TrainingDetail.php (from task 2)
- app/Livewire/LearningPath.php (from task 3)
  </read_first>
  <action>
Add to routes/web.php:
- Route: GET /trainings -> TrainingCatalog (public, auth required for enrollment)
- Route: GET /trainings/{training:slug} -> TrainingDetail (public, auth required for enrollment)
- Route: GET /trainings/{training:slug}/learn -> LearningPath (auth required, must be enrolled)
- Route: POST /trainings/{training}/enroll -> enroll action (auth required)
- Route: PUT /trainings/{training}/modules/{module}/complete -> mark complete action (auth required)
  </action>
  <acceptance_criteria>
- grep "trainings" routes/web.php returns route definitions
- Routes match the specified paths
  </acceptance_criteria>
  <verify>
    <automated>grep -q "trainings" routes/web.php && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Routes added for training catalog, detail, and learning path</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| member→view | Members view training catalog and details (public access) |
| member→enroll | Members enroll in trainings (auth required) |
| member→progress | Members mark modules complete (auth required, must be enrolled) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Elevation | LearningPath | mitigate | Verify user is enrolled before allowing module completion |
| T-10-04 | Repudiation | enrollment | mitigate | Log enrollment action with timestamps in TrainingEnrollment |
</threat_model>

<verification>
- Visit /trainings (should show catalog)
- Visit /trainings/{slug} (should show detail)
- Visit /trainings/{slug}/learn (should redirect if not enrolled)
- Test enrollment action
</verification>

<success_criteria>
- TrainingCatalog shows grid with filters working
- TrainingDetail shows hero, progress, modules, resources
- LearningPath shows sidebar navigation, content, progress
- Routes work for all pages
- Enrollment creates TrainingEnrollment record
- Module completion updates ModuleProgress
</success_criteria>

<output>
After completion, create `.planning/phases/10-learning-training-learning-paths-modules-lessons-progress-tr/10-02-SUMMARY.md`
</output>