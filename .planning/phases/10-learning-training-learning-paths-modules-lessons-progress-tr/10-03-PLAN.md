---
phase: 10
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - app/Livewire/ResourceLibrary.php
  - app/Livewire/MyProgress.php
  - resources/views/livewire/training-catalog.blade.php
  - resources/views/livewire/training-detail.blade.php
  - resources/views/livewire/learning-path.blade.php
autonomous: true
requirements: []
must_haves:
  truths:
    - Member can browse all training resources in one place
    - Member can view their progress across all enrolled trainings
  artifacts:
    - path: "app/Livewire/ResourceLibrary.php"
      provides: "Aggregated resource browser"
    - path: "app/Livewire/MyProgress.php"
      provides: "Progress tracking dashboard"
  key_links:
    - from: "ResourceLibrary"
      to: "trainings"
      via: "aggregated resources"
    - from: "MyProgress"
      to: "TrainingEnrollment"
      via: "user enrollments"
---

<objective>
Create standalone Resource Library page and Progress Tracking page for members to view their training progress.
</objective>

<context>
@.planning/phases/10-learning-training-learning-paths-modules-lessons-progress-tr/10-RESEARCH.md
@.planning/phases/10-learning-training-learning-paths-modules-lessons-progress-tr/10-UI-SPEC.md
@.planning/phases/10-learning-training-learning-paths-modules-lessons-progress-tr/10-01-PLAN.md
@.planning/phases/10-learning-training-learning-paths-modules-lessons-progress-tr/10-02-PLAN.md

# From UI-SPEC: Resource Library
- Grid layout: 3 columns desktop, 2 tablet, 1 mobile
- Resource cards: type icon (PDF/video/link), title, description
- Filter by resource type
- Search capability

# From UI-SPEC: Progress Tracking Display
- Overall progress bar by percentage
- Module completion checklist
- Time spent tracking
- Certificate eligibility status
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ResourceLibrary page component</name>
  <files>app/Livewire/ResourceLibrary.php</files>
  <read_first>
- app/Livewire/TrainingCatalog.php (existing list pattern from plan 10-02)
- 10-UI-SPEC.md (grid layout, filter by type, search, card layout)
  </read_first>
  <action>
Create ResourceLibrary Livewire component:
- Properties: search, type_filter (pdf/video/link/all), resources (collection)
- Data source: Aggregate all resources from:
  - Training resources (training->resources JSON)
  - TrainingModule resources (module->resources JSON)
- Flatten into unified resource objects with: id, type, title, description, url, source (training/module name), source_id
- Search: Filter by title, description, source name
- Type filter: Filter by resource type (pdf/video/link)
- View: Grid layout 3 cols desktop, 2 tablet, 1 mobile
- Card design: Type icon (PDF=document, Video=play, Link=link), title, description (truncated), source name
- Click card: Opens resource (PDF in modal or new tab, video in modal, link opens new tab)
- Authorization: Any authenticated member can view
  </action>
  <acceptance_criteria>
- ResourceLibrary.php exists
- Aggregates resources from all trainings and modules
- Grid layout with filter and search
- Cards show type icon, title, source
  </acceptance_criteria>
  <verify>
    <automated>ls app/Livewire/ResourceLibrary.php 2>/dev/null</automated>
  </verify>
  <done>ResourceLibrary component created</done>
</task>

<task type="auto">
  <name>Task 2: Create MyProgress page component for tracking</name>
  <files>app/Livewire/MyProgress.php</files>
  <read_first>
- app/Livewire/EventList.php (member-facing pattern)
- 10-UI-SPEC.md (progress bar, module checklist, time tracking, certificate status)
- app/Models/TrainingEnrollment.php (from plan 10-01)
- app/Models/ModuleProgress.php (from plan 10-01)
  </read_first>
  <action>
Create MyProgress Livewire component:
- Properties: enrollments ( user's TrainingEnrollments with training relationship ), overall_stats
- Calculate per enrollment:
  - Training title, category, difficulty
  - Module count (total modules in training)
  - Completed count (from ModuleProgress where status=completed)
  - Progress percent (completed/total * 100)
  - Status (enrolled/in_progress/completed)
  - Last activity (latest ModuleProgress completed_at)
- Overall stats:
  - Total trainings enrolled
  - Total modules completed
  - Average progress across all enrollments
  - Trainings completed count (progress = 100%)
- View layout:
  - Summary cards at top: Enrolled count, Completed count, Avg progress
  - List of enrolled trainings with progress bars
  - Each item: Expand to show module-level checklist
- Certificate eligibility: Show if any training is completed (links to Phase 11 certificate)
- Time tracking: Show total time estimated from completed modules (duration_minutes sum)
  </action>
  <acceptance_criteria>
- MyProgress.php exists
- Shows list of enrolled trainings with progress
- Shows overall statistics
- Module-level checklist expandable
  </acceptance_criteria>
  <verify>
    <automated>ls app/Livewire/MyProgress.php 2>/dev/null</automated>
  </verify>
  <done>MyProgress component created</done>
</task>

<task type="auto">
  <name>Task 3: Add routes for Resource Library and My Progress</name>
  <files>routes/web.php</files>
  <read_first>
- routes/web.php (existing)
- app/Livewire/ResourceLibrary.php (from task 1)
- app/Livewire/MyProgress.php (from task 2)
  </read_first>
  <action>
Add to routes/web.php:
- Route: GET /resources -> ResourceLibrary (auth required)
- Route: GET /my-progress -> MyProgress (auth required)
  </action>
  <acceptance_criteria>
- grep "resources" routes/web.php returns route
- grep "my-progress" routes/web.php returns route
  </acceptance_criteria>
  <verify>
    <automated>grep -q "resources" routes/web.php && grep -q "my-progress" routes/web.php && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Routes added for Resource Library and My Progress</done>
</task>

<task type="auto">
  <name>Task 4: Update TrainingCatalog to link to Resource Library</name>
  <files>resources/views/livewire/training-catalog.blade.php</files>
  <read_first>
- resources/views/livewire/event-list.blade.php (existing list view pattern)
- app/Livewire/TrainingCatalog.php (from plan 10-02)
  </read_first>
  <action>
In TrainingCatalog view:
- Add navigation link to Resource Library in page header
- Add "My Progress" link in user navigation area
- Consider adding quick-access cards for: "Browse Resources", "My Progress", "Continue Learning"
  </action>
  <acceptance_criteria>
- View has links to /resources and /my-progress
- Navigation visible to authenticated users
  </acceptance_criteria>
  <verify>
    <automated>grep -q "resources" resources/views/livewire/training-catalog.blade.php 2>/dev/null || echo "View may need update"</automated>
  </verify>
  <done>Navigation links added</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| member→progress | Members view their own progress (auth required) |
| member→resources | Members view resource library (auth required) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Information Disclosure | MyProgress | mitigate | Ensure user can only see their own progress, not other users' |
</threat_model>

<verification>
- Visit /resources (should show all resources aggregated)
- Visit /my-progress (should show user's enrollments and progress)
- Test filter and search on Resource Library
- Verify progress calculation is correct
</verification>

<success_criteria>
- Resource Library shows aggregated resources from all trainings
- Filter by type (PDF/Video/Link) works
- Search works
- My Progress shows all user's enrollments
- Progress percentages calculated correctly
- Module-level checklist visible
- Routes work for both pages
</success_criteria>

<output>
After completion, create `.planning/phases/10-learning-training-learning-paths-modules-lessons-progress-tr/10-03-SUMMARY.md`
</output>