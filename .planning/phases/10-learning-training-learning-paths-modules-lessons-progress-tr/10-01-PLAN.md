---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Models/Training.php
  - app/Models/TrainingModule.php
  - app/Models/TrainingEnrollment.php
  - app/Models/ModuleProgress.php
  - app/Livewire/Admin/TrainingList.php
  - app/Livewire/Admin/TrainingForm.php
  - app/Livewire/Admin/ModuleForm.php
autonomous: true
requirements: []
must_haves:
  truths:
    - Admin can create, edit, and delete trainings with modules
    - Training model has proper relationships to modules and enrollments
    - Admin UI follows existing patterns from Event admin
  artifacts:
    - path: "app/Models/Training.php"
      provides: "Training entity with relationships"
    - path: "app/Livewire/Admin/TrainingList.php"
      provides: "Admin training list with CRUD actions"
  key_links:
    - from: "app/Models/Training"
      to: "training_modules"
      via: "hasMany"
    - from: "app/Livewire/Admin/TrainingForm"
      to: "app/Models/Training"
      via: "save action"
---

<objective>
Build core Training models with relationships, fillable properties, and admin Livewire components for managing trainings and modules.
</objective>

<context>
@.planning/phases/10-learning-training-learning-paths-modules-lessons-progress-tr/10-RESEARCH.md
@.planning/phases/10-learning-training-learning-paths-modules-lessons-progress-tr/10-UI-SPEC.md

# From UI-SPEC: Admin Training Management
- Table: title, category, difficulty, enrollments, status
- Actions: Create, Edit, Delete
- Publish/Unpublish toggle
- View enrollments per training
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Training model with relationships and fillable</name>
  <files>app/Models/Training.php</files>
  <read_first>
- database/migrations/2025_12_01_145629_create_trainings_table.php (existing schema)
- app/Models/Event.php (existing model pattern with relationships)
  </read_first>
  <action>
Add to Training model:
- $fillable: title, slug, description, category, difficulty, objectives, prerequisites, duration_hours, thumbnail, resources, max_enrollments, is_published, available_from, available_until, instructor_id
- relationships: modules() (hasMany TrainingModule), enrollments() (hasMany TrainingEnrollment), instructor() (belongsTo User)
- scopes: published(), byCategory(), byDifficulty()
- Slug should be auto-generated from title using Str::slug()
  </action>
  <acceptance_criteria>
- grep "title.*fillable" app/Models/Training.php returns $fillable array with title, slug, description
- grep "function modules" app/Models/Training.php returns hasMany relationship
- grep "function enrollments" app/Models/Training.php returns hasMany relationship
  </acceptance_criteria>
  <verify>
    <automated>grep -q "title.*fillable" app/Models/Training.php && grep -q "function modules" app/Models/Training.php && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Training model has $fillable, relationships (modules, enrollments, instructor), and scopes</done>
</task>

<task type="auto">
  <name>Task 2: Define TrainingModule model with relationships and fillable</name>
  <files>app/Models/TrainingModule.php</files>
  <read_first>
- database/migrations/2025_12_01_145813_create_training_modules_table.php (existing schema)
- app/Models/Exam.php (existing model pattern)
  </read_first>
  <action>
Add to TrainingModule model:
- $fillable: training_id, title, content, order, duration_minutes, resources
- relationships: training() (belongsTo Training), progress() (hasMany ModuleProgress)
- Scope: ordered() for ordering by order field
  </action>
  <acceptance_criteria>
- grep "title.*fillable" app/Models/TrainingModule.php returns $fillable
- grep "function training" app/Models/TrainingModule.php returns belongsTo relationship
  </acceptance_criteria>
  <verify>
    <automated>grep -q "title.*fillable" app/Models/TrainingModule.php && grep -q "function training" app/Models/TrainingModule.php && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>TrainingModule model has $fillable and relationships</done>
</task>

<task type="auto">
  <name>Task 3: Define TrainingEnrollment and ModuleProgress models</name>
  <files>app/Models/TrainingEnrollment.php, app/Models/ModuleProgress.php</files>
  <read_first>
- app/Models/EventRegistration.php (similar pattern)
- app/Models/ExamAttempt.php (user enrollment pattern)
  </read_first>
  <action>
TrainingEnrollment model:
- $fillable: training_id, user_id, enrolled_at, status (enrolled/completed/dropped)
- relationships: training() (belongsTo Training), user() (belongsTo User)
- Unique constraint: training_id + user_id

ModuleProgress model:
- $fillable: user_id, module_id, status (not_started/in_progress/completed), completed_at
- relationships: user() (belongsTo User), module() (belongsTo TrainingModule)
- Unique constraint: user_id + module_id
  </action>
  <acceptance_criteria>
- TrainingEnrollment has training_id, user_id in $fillable
- ModuleProgress has user_id, module_id, status in $fillable
- Both have user() and training/module relationships
  </acceptance_criteria>
  <verify>
    <automated>grep -q "training_id.*fillable" app/Models/TrainingEnrollment.php && grep -q "user_id.*fillable" app/Models/ModuleProgress.php && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>TrainingEnrollment and ModuleProgress models defined with relationships</done>
</task>

<task type="auto">
  <name>Task 4: Create admin Livewire components for Training CRUD</name>
  <files>app/Livewire/Admin/TrainingList.php, app/Livewire/Admin/TrainingForm.php</files>
  <read_first>
- app/Livewire/Admin/EventList.php (existing admin pattern)
- app/Livewire/Admin/EventForm.php (existing form pattern)
- 10-UI-SPEC.md (admin table columns: title, category, difficulty, enrollments, status)
  </read_first>
  <action>
TrainingList:
- Use Filament tables pattern or Livewire tables
- Columns: title, category (with color badges per UI-SPEC), difficulty (with color), enrollments count, is_published (toggle)
- Actions: Create, Edit, Delete
- Filters: category, difficulty, published status

TrainingForm:
- Fields: title (text), slug (auto-generated), description (textarea), category (select from enum), difficulty (select), objectives (textarea), prerequisites (textarea), duration_hours (number), thumbnail (file), resources (JSON), max_enrollments (number), is_published (toggle), available_from (date), available_until (date), instructor_id (select user)
- Validation rules per field
- Save creates/updates Training and related TrainingModules
  </action>
  <acceptance_criteria>
- TrainingList.php exists with table columns matching UI-SPEC
- TrainingForm.php exists with all fields from action
- Both use Livewire patterns consistent with existing admin components
  </acceptance_criteria>
  <verify>
    <automated>ls app/Livewire/Admin/TrainingList.php app/Livewire/Admin/TrainingForm.php 2>/dev/null | wc -l</automated>
  </verify>
  <done>Admin Training CRUD components created</done>
</task>

<task type="auto">
  <name>Task 5: Create admin Livewire component for Module management</name>
  <files>app/Livewire/Admin/ModuleForm.php</files>
  <read_first>
- app/Livewire/Admin/EventForm.php (existing form pattern)
- app/Livewire/Admin/TrainingForm.php (from task above)
  </read_first>
  <action>
ModuleForm:
- Fields: training_id (select), title (text), content (textarea), order (number), duration_minutes (number), resources (JSON array: type, url, title)
- Reorder capability for modules within a training
- Save creates/updates TrainingModule
  </action>
  <acceptance_criteria>
- ModuleForm.php exists with required fields
- Has training relationship selection
  </acceptance_criteria>
  <verify>
    <automated>ls app/Livewire/Admin/ModuleForm.php 2>/dev/null</automated>
  </verify>
  <done>Admin ModuleForm component created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin→training_crud | Admin creates/edits training content (trusted admin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | TrainingForm | mitigate | Validate all inputs with Form Request, sanitize slug generation |
| T-10-02 | Information Disclosure | TrainingList | mitigate | Ensure only authorized admins can view via auth middleware |
</threat_model>

<verification>
- Run: `php artisan model:show Training` to verify model has relationships
- Run: `php artisan livewire:list` to verify components registered
- Check: Database has training_tables accessible
</verification>

<success_criteria>
- Training model has $fillable, relationships, scopes
- TrainingModule model has $fillable, relationships
- TrainingEnrollment and ModuleProgress models have $fillable and relationships
- Admin TrainingList shows table with all required columns
- Admin TrainingForm creates/edits trainings
- Admin ModuleForm creates/edits modules
</success_criteria>

<output>
After completion, create `.planning/phases/10-learning-training-learning-paths-modules-lessons-progress-tr/10-01-SUMMARY.md`
</output>